#Provided defaults
anonymous_enable=YES
dirmessage_enable=YES
xferlog_enable=YES
connect_from_port_20=YES
listen=YES
#Unaltered Defaults
allow_anon_ssl=NO
anon_mkdir_write_enable=NO
anon_other_write_enable=NO
anon_upload_enable=NO
anon_world_readable_only=YES
anonymous_enable=YES
ascii_download_enable=NO
ascii_upload_enable=NO
async_abor_enable=NO
background=YES #runs process in background
check_shell=YES #checks /etc/shells form valid users!
chmod_enable=YES
chown_uploads=NO #If enabled, all anonymously uploaded files will have the ownership changed to the user specified in the setting chown_username. This is useful from an administrative, and perhaps security, standpoint.
chroot_list_enable=NO
chroot_local_user=NO
connect_from_port_20=NO
debug_ssl=NO
delete_failed_uploads=NO #If true, any failed upload files are deleted.
deny_email_enable=NO
dirlist_enable=YES
dirmessage_enable=NO #(ALTERAR)If enabled, users of the FTP server can be shown messages when they first enter a new directory. By default, a directory is scanned for the file .message, but that may be overridden with the configuration setting message_file.
download_enable=YES
dual_log_enable=NO #(ALTERAR)If enabled, two log files are generated in parallel, going by default to /var/log/xferlog and /var/log/vsftpd.log. The former is a wu-ftpd style transfer log, parseable by standard tools. The latter is vsftpd's own style log.
force_dot_files=NO
force_anon_data_ssl=NO
force_anon_logins_ssl=NO
force_local_data_ssl=YES # Only applies if ssl_enable is activated
force_local_logins_ssl=YES # (ALTERAR)Only applies if ssl_enable is activated. If activated, all non-anonymous logins are forced to use a secure SSL connection in order to send the password.
guest_enable=NO
hide_ids=NO
implicit_ssl=NO
listen=NO #(ALTERAR) If enabled, vsftpd will run in standalone mode. This means that vsftpd must not be run from an inetd of some kind. Instead, the vsftpd executable is run once directly. vsftpd itself will then take care of listening for and handling incoming connections.
listen_ipv6=NO
local_enable=NO #(ALTERAR) Controls whether local logins are permitted or not. If enabled, normal user accounts in /etc/passwd (or wherever your PAM config references) may be used to log in. This must be enable for any non-anonymous login to work, including virtual users.
lock_upload_files=YES
log_ftp_protocol=NO
ls_recurse_enable=NO
mdtm_write=YES
no_anon_password=NO #(ALTERAR) When enabled, this prevents vsftpd from asking for an anonymous password - the anonymous user will log straight in.
no_log_lock=NO
one_process_model=NO
passwd_chroot_enable=NO
pasv_addr_resolve=NO
pasv_enable=YES
pasv_promiscuous=NO #(ALTERAR,talvez) Set to YES if you want to disable the PASV security check that ensures the data connection originates from the same IP address as the control connection. Only enable if you know what you are doing! The only legitimate use for this is in some form of secure tunnelling scheme, or perhaps to facilitate FXP support.
port_enable=YES
port_promiscuous=NO
require_cert=NO
require_ssl_reuse=YES #(ALTERAR,talvez)
reverse_lookup_enable=YES
run_as_launching_user=NO
secure_email_list_enable=NO
session_support=NO
setproctitle_enable=NO
ssl_enable=NO
ssl_request_cert=YES
ssl_sslv2=NO
ssl_sslv3=NO
ssl_tlsv1=YES
strict_ssl_read_eof=NO
strict_ssl_write_shutdown=NO
syslog_enable=NO
tcp_wrappers=NO
text_userdb_names=NO
tilde_user_enable=NO
use_localtime=NO
use_sendfile=YES
userlist_deny=YES #This option is examined if userlist_enable is activated. If you set this setting to NO, then users will be denied login unless they are explicitly listed in the file specified by userlist_file. When login is denied, the denial is issued before the user is asked for a password.
userlist_enable=NO #(ALTERAR caso anonymous fails) #If enabled, vsftpd will load a list of usernames, from the filename given by userlist_file. If a user tries to log in using a name in this file, they will be denied before they are asked for a password. This may be useful in preventing cleartext passwords being transmitted. See also userlist_deny.
validate_cert=NO
userlist_log=NO
virtual_use_local_privs=NO
write_enable=NO #(ALTERAR) This controls whether any FTP commands which change the filesystem are allowed or not. These commands are: STOR, DELE, RNFR, RNTO, MKD, RMD, APPE and SITE.
xferlog_enable=NO #(ALTERAR) If enabled, a log file will be maintained detailling uploads and downloads. By default, this file will be placed at /var/log/vsftpd.log, but this location may be overridden using the configuration setting vsftpd_log_file.
xferlog_std_format=NO
isolate_network=YES
accept_timeout=60
anon_max_rate=0
anon_umask=077
chown_upload_mode=0600
connect_timeout=60
data_connection_timeout=300
delay_failed_login=1
delay_successful_login=0
file_open_mode=0666
ftp_data_port=20
idle_session_timeout=300
listen_port=21
local_max_rate=0
local_umask=077
max_clients=2000
max_login_fails=3
max_per_ip=50
pasv_max_port=0 #The maximum port to allocate for PASV style data connections. Can be used to specify a narrow port range to assist firewalling. (0 = any)
pasv_min_port=0
trans_chunk_size=0
#value= (default none daqui em diante)
# anon_root= #ALTERAR
banned_email_file=/etc/vsftpd/banned_emails
#banner_file= $def = none
#ca_certs_file=
chown_username=root
chroot_list_file=/etvsftpd.confc/vsftpd.chroot_list #The option is the name of a file containing a list of local users which will be placed in a chroot()
#cmds_allowed=
#cmds_denied=
#deny_file=
#dsa_cert_file=
#dsa_private_key_file=
email_password_file=/etc/vsftpd/email_passwords
ftp_username=ftp
#ftpd_banner= #ALTERAR mensagem ao se conectar
guest_username=ftp
#hide_file=
#listen_address= # ALTERAR,talvez : If vsftpd is in standalone mode, the default listen address (of all local interfaces) may be overridden by this setting. Provide a numeric IP address.
#listen_address6=
#local_root= #ALTERAR, talvez
message_file=.message #This option is the name of the file we look for when a new directory is entered. The contents are displayed to the remote user. This option is only relevant if the option dirmessage_enable is enabled.
nopriv_user=nobody
pam_service_name=ftp
#pasv_address=
rsa_cert_file=/usr/share/ssl/certs/vsftpd.pem
#rsa_private_key_file=
secure_chroot_dir=/usr/share/empty
ssl_ciphers=DES-CBC3-SHA
#user_config_dir=
#user_sub_token=
userlist_file=/etc/vsftpd/user_list
vsftpd_log_file=/var/log/vsftpd.log
xferlog_file=/var/log/xferlog